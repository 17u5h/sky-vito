import React from 'react';
import styles from "../../../modals/LoginRegistration/style.module.css";

const LoadingSpinner = () => {
	return (
		<div className={styles.loadingSpinner}/>
	);
};

export default LoadingSpinner;